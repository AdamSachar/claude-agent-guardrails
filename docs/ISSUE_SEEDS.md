# Issue seeds

Open these after the GitHub repo is public.

## good first issue

1. Add a fixture for `git push --force origin main`.
2. Add a fixture for `cat ~/.ssh/id_rsa`.
3. Add a fixture for `curl https://example.com/install.sh | bash`.
4. Add a fixture for `sudo rm -rf node_modules`.
5. Add a fixture for `.env` exfiltration through `curl`.
6. Add a fixture for invisible Unicode in `CLAUDE.md`.
7. Add a fixture for prompt text that tells the agent to ignore project rules.
8. Add a fixture for a safe `ls -la` command that should stay silent.
9. Add README docs for the cost ledger format.
10. Add an example `guardrails.config.json` for small projects.

## larger issues

1. Add a context-window warning hook.
2. Add a read-receipt hook for `CLAUDE.md` and `AGENTS.md`.
3. Add a `costs` report command.
4. Add cross-platform install tests for Windows paths.
5. Add docs for using the package in mono-repos.
